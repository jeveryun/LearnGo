package main

func nonRepeat(s string) int {
	lastOccurred := make(map[byte]int)
	start := 0
	maxLength := 0
	for i, ch := range []byte(s) {
		println(lastOccurred[ch])
		if lastI, ok := lastOccurred[ch]; ok && lastI >= start {
			start = lastI + 1
		}
		if i-start+1 > maxLength {
			maxLength = i - start + 1
		}
		lastOccurred[ch] = i
	}
	return maxLength
}

func main() {
	println(nonRepeat("abcabcbb"))
	// println(nonRepeat("bbbb"))
	// println(nonRepeat("pwwwkew"))
}